# Client-side (Vite) environment variables.
# NOTE: VITE_* values are bundled into the browser build. Do NOT put secrets here.

# Supabase project URL (e.g. https://xxxx.supabase.co)
VITE_SUPABASE_URL=

# Supabase public anon/publishable key (safe to expose; required by supabase-js in the browser)
# Use either of these (the code supports both):
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Optional: public app origin (used for links/redirects in some setups)
VITE_APP_ORIGIN=
